<div class="navbar navbar-inverse navbar-static-top" role="navigation">
    <div class="container">
        <!-- Shown when collapse is hidden (nav does not fit in window) -->
        <div class="navbar-header">
            <a class="navbar-brand" href="<%= localUrl('/') %>"><%- appName %></a>

            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>

        <!-- Collapse elements / navbar content -->
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
               <li><a href="<%= localUrl('/app') %>">Angular test</a></li>
            </ul>

            <form class="navbar-form navbar-right" role="form" action="<%= localUrl('/auth/local') %>" method="post">
                <div class="form-group">
                    <input name="username" type="text" placeholder="Username" class="form-control">
                </div>
                <div class="form-group">
                    <input name="password" type="password" placeholder="Password" class="form-control">
                </div>
                <button type="submit" class="btn btn-success">Sign in</button>
            </form>
        </div><!--/.navbar-collapse -->
    </div>
</div>